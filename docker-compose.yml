version: "3"

services:
  api:
    build: .
    volumes:
      - .:/app
    env_file:
      - .env
    ports:
      - ${PORT}:${PORT}
    command: sh -c 'npm install nodemon -g && npm i && nodemon start'
